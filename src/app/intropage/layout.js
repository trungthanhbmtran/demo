import Category from "../../components/Layout/(CategoryBox)/Category"

const Layout = ({children}) =>{
    return(
        <Category>
            {children}
        </Category>
    )
}

export default Layout